# 2026-08-03

## rebuild 最小可运行流程 —— 测试全部通过
- 修复 `RbSceneRegistry.get_path` 静态方法命名冲突：`class_name` 脚本（默认继承 RefCounted→Object）的静态方法名若与 Object 内置方法同名（如 `get_path`，0 参数），GDScript 会解析到继承的 0 参数版本并报错 "Expected 0 argument(s)"。
  - 修复方式：重命名为 `resolve_path`，同步更新 `rb_scene_loader.gd` 与 `rb_flow_test.gd` 三处调用。
- 用 Godot 4.6.2 控制台版跑通 headless 冒烟测试 `rebuild/tools/rb_flow_test.gd`：**38/38 PASS**。
  - 完整链路：主菜单→新游戏→序章街道→开场对话→走到公寓门→开门进房间→房间开场对话→查看手机(首看/重复文本分支+剧情标记)→返回街道→主菜单。
- Godot 可执行文件定位：桌面 `.lnk`（`C:\Users\25450\Desktop\Godot_v4.6.2-stable_win64.exe.lnk`）指向 `E:\Godot\Godot_v4.6.2-stable_win64.exe`（标准 GUI 版）。headless 测试需用同目录的 `_console.exe` 才有标准输出。
- 交互探测器（`RbInteractionSensor`, Area2D, collision_mask=8, monitoring 默认 true）能正常锁定门/手机：交互物 Area2D 设 `monitoring=false` 不影响被探测，只要 `monitorable` 默认 true 且 layer 与 sensor mask 重叠即可。

## 待确认（用户要求不要擅自迁移其他关卡）
- 已按用户要求停在"最小可运行流程"阶段，等待用户确认后再迁移其他关卡。
- 用户额外提供了 Godot MCP 地址 `http://localhost:9086/mcp`，尚未配置到 WorkBuddy（未明确要求）。

## 用户报告"进不去公寓" —— 根因定位（2026-08-04）
- 用户手动游玩时"进不去公寓"。排查结论：**不是新 rebuild 系统的 bug**。
- 关键证据：`project.godot` 的 `run/main_scene = "res://scenes/main_menu.tscn"`（旧系统主菜单）。按 F5 / Play 跑的是**旧系统**，其门系统已被审计报告判为损坏。新 rebuild 入口是 `rebuild/ui/rb_main_menu.tscn`。
- 为彻底排除新系统输入链路问题，把冒烟测试里"直接 emit 信号"的 3 处交互改成**模拟真实 Space 按键**（`InputEventKey` + `Input.parse_input_event` → 传感器 `_unhandled_input` → `interact_requested`）。重跑仍 **38/38 PASS**。证明 新系统"走到门→按空格→切场景"整条输入链路在真实输入下也通。
- 用户正确手动测试方式：在 Godot 编辑器里打开 `rebuild/ui/rb_main_menu.tscn`，右键→"运行场景"（或播放下拉里选 Run Current Scene），**不要直接按 F5**（F5 跑旧系统）。进门 = 靠近门出现"进入公寓"提示后按空格。
- 交互/对话键位（已确认 project.godot）：`interact`=空格、`dialogue_advance`=空格、`ui_cancel`=Esc、`move_*`=WASD。

## Godot MCP 连接状态
- 探测 `http://localhost:9086/mcp`：**TCP 不通（无法连接到远程服务器）**。服务器当前未运行。
- 需用户先启动 Godot MCP 服务（打开带 MCP 插件的 Godot 编辑器 / 或单独启动 MCP server）后，我再把它写进 `~/.workbuddy/mcp.json` 并接上，之后即可由我自行驱动游戏复现问题。
